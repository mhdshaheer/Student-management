<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student Management System - Red Theme</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        :root {
            --primary: #ef4444;
            --secondary: #f97316;
            --gradient: linear-gradient(135deg, var(--primary), var(--secondary));
        }

        body {
            background: #fef2f2;
            min-height: 100vh;
        }

        .gradient-bg {
            background: var(--gradient);
        }

        .glass-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(239, 68, 68, 0.1);
            box-shadow: 0 8px 32px rgba(239, 68, 68, 0.1);
        }

        .form-input {
            background: rgba(255, 255, 255, 0.9);
            border: 2px solid #fee2e2;
            transition: all 0.3s ease;
            @apply block w-full rounded-xl px-4 py-3;
        }

        .form-input:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(239, 68, 68, 0.1);
            outline: none;
        }

        .form-select {
            background: rgba(255, 255, 255, 0.9);
            border: 2px solid #fee2e2;
            transition: all 0.3s ease;
            @apply block w-full rounded-xl px-4 py-3;
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' fill='none' viewBox='0 0 20 20'%3e%3cpath stroke='%23ef4444' stroke-linecap='round' stroke-linejoin='round' stroke-width='1.5' d='M6 8l4 4 4-4'/%3e%3c/svg%3e");
            background-position: right 1rem center;
            background-repeat: no-repeat;
            background-size: 1.5em 1.5em;
            padding-right: 2.5rem;
            -webkit-appearance: none;
            -moz-appearance: none;
            appearance: none;
        }

        .form-select:focus {
            border-color: var(--primary);
            box-shadow: 0 0 0 4px rgba(239, 68, 68, 0.1);
            outline: none;
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-20px); }
        }

        .btn-gradient {
            background: var(--gradient);
            color: white;
            transition: all 0.3s ease;
        }

        .btn-gradient:hover {
            transform: translateY(-2px);
            box-shadow: 0 10px 20px rgba(239, 68, 68, 0.2);
        }

        .form-label {
            @apply text-gray-700 font-medium mb-2 block;
            font-size: 0.95rem;
        }

        .nav-item {
            @apply px-4 py-2 rounded-lg transition-all duration-300;
        }

        .nav-item:hover {
            background: rgba(255, 255, 255, 0.2);
            transform: translateY(-2px);
        }

        .shape {
            position: absolute;
            border-radius: 50%;
            background: linear-gradient(45deg, var(--primary), var(--secondary));
            opacity: 0.1;
        }

        .shape-1 {
            width: 300px;
            height: 300px;
            top: -150px;
            left: -150px;
        }

        .shape-2 {
            width: 200px;
            height: 200px;
            bottom: -100px;
            right: -100px;
        }

        .status-badge {
            @apply px-3 py-1 rounded-full text-sm font-medium;
            background: rgba(239, 68, 68, 0.1);
            color: var(--primary);
        }
    </style>
</head>
<body class="relative">
    <div class="absolute inset-0 overflow-hidden">
        <div class="shape shape-1 animate-float"></div>
        <div class="shape shape-2 animate-float" style="animation-delay: -3s;"></div>
    </div>

    <!-- Navigation -->
    <nav class="gradient-bg text-white sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-6 py-4">
            <div class="flex items-center justify-between">
                <div class="flex items-center space-x-4">
                    <div class="text-2xl font-bold">
                        <i class="fas fa-graduation-cap mr-2"></i>
                        Student Management
                    </div>
                    <span class="status-badge">Beta</span>
                </div>
                <div class="flex items-center space-x-2">
                    <a href="#" class="nav-item">Dashboard</a>
                    <a href="/" class="nav-item">Students</a>
                    <a href="#" class="nav-item">Courses</a>
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main class="max-w-4xl mx-auto px-6 py-12">
        <div class="glass-card rounded-2xl p-8">
            <div class="flex items-center justify-between mb-8">
                <div>
                    <h1 class="text-3xl font-bold bg-gradient-to-r from-primary to-secondary bg-clip-text text-gray-800">
                        Student Enrollment
                    </h1>
                    <p class="text-gray-600 mt-2">Begin your academic journey with us</p>
                </div>
                <div class="w-16 h-16 rounded-xl gradient-bg flex items-center justify-center text-white">
                    <i class="fas fa-user-graduate text-2xl"></i>
                </div>
            </div>

            <form class="space-y-8" action="/addStudent" method="post">
                <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
                    <!-- Name Field -->
                    <div class="space-y-2">
                        <label class="form-label">
                            <i class="fas fa-user text-red-500 mr-2"></i>
                            Full Name
                        </label>
                        <input type="text" class="form-input" name="fullName" placeholder="Enter your full name" required>
                    </div>

                    <!-- Email Field -->
                    <div class="space-y-2">
                        <label class="form-label">
                            <i class="fas fa-envelope text-red-500 mr-2"></i>
                            Email Address
                        </label>
                        <input type="email" class="form-input" name="email" placeholder="your@email.com" required>
                    </div>

                    <!-- Place Field -->
                    <div class="space-y-2">
                        <label class="form-label">
                            <i class="fas fa-map-marker-alt text-red-500 mr-2"></i>
                            Place
                        </label>
                        <input type="text" class="form-input" name="place" placeholder="City/Country" required>
                    </div>

                    <!-- Course Field -->
                    <div class="space-y-2">
                        <label class="form-label">
                            <i class="fas fa-book text-red-500 mr-2"></i>
                            Select Course
                        </label>
                        <select class="form-select" name="course" required>
                            <option value="">Choose your course</option>
                            <option value="computer_science">💻 Computer Science</option>
                            <option value="engineering">⚡ Engineering</option>
                            <option value="business">📊 Business</option>
                            <option value="arts">🎨 Arts</option>
                        </select>
                    </div>

                    <!-- Course Duration Field -->
                    <div class="space-y-2">
                        <label class="form-label">
                            <i class="fas fa-clock text-red-500 mr-2"></i>
                            Duration
                        </label>
                        <select class="form-select" name="duration" required>
                            <option value="">Select duration</option>
                            <option value="6 months">6 Months</option>
                            <option value="1 year">1 Year</option>
                            <option value="2 years">2 Years</option>
                            <option value="3 years">3 Years</option>
                            <option value="4 years">4 Years</option>
                        </select>
                    </div>
                </div>

                <!-- Form Actions -->
                <div class="flex items-center justify-between pt-6 border-t border-red-100">
                    <button type="button" onclick="window.location.href='/'" class="px-6 py-3 rounded-xl bg-red-50 text-red-600 hover:bg-red-100 transition-all duration-300">
                        <i class="fas fa-arrow-left mr-2"></i>
                        Back
                    </button>
                    <button type="submit" class="btn-gradient px-8 py-3 rounded-xl font-medium">
                        <i class="fas fa-paper-plane mr-2"></i>
                        Submit Application
                    </button>
                </div>
            </form>
        </div>

        <!-- Progress Card -->
        <!-- <div class="glass-card mt-6 p-4 rounded-xl flex items-center justify-between">
            <div class="flex items-center space-x-4">
                <div class="w-10 h-10 rounded-full gradient-bg flex items-center justify-center text-white">
                    <i class="fas fa-chart-line"></i>
                </div>
                <div>
                    <h3 class="font-medium text-gray-800">Application Progress</h3>
                    <p class="text-sm text-gray-600">Step 1 of 2 - Basic Information</p>
                </div>
            </div>
            <div class="w-32 h-2 bg-red-100 rounded-full overflow-hidden">
                <div class="w-1/2 h-full gradient-bg"></div>
            </div>
        </div> -->
    </main>
</body>
</html>